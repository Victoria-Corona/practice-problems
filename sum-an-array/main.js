function sumArray(numbersArray){

    var sum = 0;

  for(var count = 0; count < numbersArray.length; count++){
    sum += numbersArray[count]
  }
  return sum;
}


//make a function
//create a for loop that will go through the array
//add them numbers
//have a variable to store them numbers
//goal is to return the numbers added together
