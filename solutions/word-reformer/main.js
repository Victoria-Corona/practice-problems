function wordReformer(wordFragment1, wordFragment2) {
  return wordFragment1 + wordFragment2;
}

var wordReformerResults = wordReformer("bro", "ken");
console.log(wordReformerResults);

// add commit push origin word-reformer in master FOR NOW
